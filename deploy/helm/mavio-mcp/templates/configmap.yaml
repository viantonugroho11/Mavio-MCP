apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mavio-mcp.fullname" . }}-config
  labels: {{- include "mavio-mcp.labels" . | nindent 4 }}
data:
  mavio.config.yaml: |
{{ .Values.config.yaml | indent 4 }}
