{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mavio-mcp.serviceAccountName" . }}
  labels: {{- include "mavio-mcp.labels" . | nindent 4 }}
{{- end }}
