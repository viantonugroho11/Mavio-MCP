apiVersion: v1
kind: Service
metadata:
  name: mavio-mcp
  labels: { app.kubernetes.io/name: mavio-mcp }
spec:
  type: ClusterIP
  ports:
    - { name: http, port: 4000, targetPort: http, protocol: TCP }
  selector: { app.kubernetes.io/name: mavio-mcp }
